import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ahsanmahmood.cap_firebase',
  appName: 'capacitor-firebase-kit',
  webDir: 'dist',
};

export default config;
